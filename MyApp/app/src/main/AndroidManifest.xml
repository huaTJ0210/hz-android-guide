<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hz.myapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.AlertActivity"></activity>
        <activity android:name=".criminal.intent.CrimePagerActivity" />
        <activity android:name=".criminal.intent.CrimeActivity" />
        <activity android:name=".criminal.intent.CrimeListActivity" />
        <activity android:name=".activity.QuizActivity" />
        <activity android:name=".activity.DemoActivity" />
        <activity android:name=".activity.SecondActivity" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.FirstActivity" />
    </application>

</manifest>